I want you to adjust [@page.tsx](file:///Users/lorenzkrinner/Dev/hackathons/ehl_hack/app/chat/page.tsx)  so that we start the mock loop with a real composer, it should be very similar to all other harnesses (like in the image bwlo) just iwhtout all the fance stuff. 

In there we have a "+" button where you can select from the mock list of instances. This simply firs opens a popover with just two option for now "select incident" and "select file", file does nothing, incident opens a dialogue (default size) with a list of all incidents, when I selcted a incident it should be tagged to my cursor and should look like the image I uploaded

Then and only then should the deterministic flow start. Right now in the dropdown I can only select the instance where our flow is defined in. All others are disabled.