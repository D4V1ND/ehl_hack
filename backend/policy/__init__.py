"""Policy: the four rules, read off the company profile.

Nothing here does I/O and nothing here decides what to buy. `rules` answers
"may we buy from this supplier at all", `report` renders the answer as the
markdown a reviewer reads in the pull request.
"""
