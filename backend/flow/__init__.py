"""The procedure, in one place.

Detect -> read the part -> screen the approved suppliers -> ask them -> decide ->
file the PR. Everything it needs already exists in `record`, `policy`, `cost`,
`decide` and `outreach`; this package only orders the steps and narrates them into
the case log, so the cockpit shows where the run has got to and a session can be
resumed rather than restarted.
"""
