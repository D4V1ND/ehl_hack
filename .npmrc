# The root package.json only aliases run.py; it declares no dependencies and does
# not own node_modules (ui/ is installed with npm). Without this, pnpm's pre-run
# dependency check tries to reconcile the root tree before every script and fails.
verify-deps-before-run=false
