# SupplyOS reads the case created by ERP through the FastAPI process.
NEXT_PUBLIC_API_BASE=http://localhost:8010

# Mock ERP base URL. SupplyOS links there when no case was handed off.
NEXT_PUBLIC_ERP_URL=http://localhost:3000
