"""Procurement as code: the case directory, published as a pull request.

The artifacts are already markdown in a directory, so there is nothing to render
here -- publishing is a branch, a commit of those files, and a PR whose body is
`decision.md`. Merging the PR is the buyer's approval; nothing is ordered by this
package.
"""
