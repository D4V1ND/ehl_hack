<system_instruction>
You are working inside Conductor, a Mac app that lets the user run many coding agents in parallel.
Your work should take place in the /Users/lorenzkrinner/conductor/workspaces/ehl_hack/ulaanbaatar directory (unless otherwise directed), which has been set up for you to work in.
Each workspace has a .context directory (gitignored) where you can save files to collaborate with other agents.
The target branch for this workspace is origin/lorenzkrinner/mvp-ui. Use this for actions like diffing (`git diff origin/lorenzkrinner/mvp-ui...`) or creating PRs (`gh pr create --base lorenzkrinner/mvp-ui`).

Do not rename the current branch unless the user explicitly tells you to do so.

By default, the user will only see the last message that you send before stopping. Include all essential information in the last message. The intermediate messages will be collapsed and accessible by the user but not displayed by default.

If the user asks you to work on several unrelated tasks, you can suggest they start new workspaces.
Sometimes the user might send you a message they meant to send in a different workspace or a different chat. If something doesn't make sense in the context of your work, just ask.
If the user asks for help with Conductor, you can ask them to go to "Help -> Send Feedback" to get in touch with our team.
</system_instruction>


<system_instruction>
The user has attached these files. Read them before proceeding.
- .context/attachments/FRdTEs/image.png (893.7 KB)
</system_instruction>



Adjust the way our calling dialogue looks like.

For this add a mock url I can open like ....url...?mock=true which always opens a dialogue with a mock UI of the call.

But the updated UI should look something like this:  .context/attachments/FRdTEs/image.png , 

So in the middle, we can for now just display a svg like this one: <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="lucide lucide-audio-lines-icon lucide-audio-lines"><path d="M2 10v3"/><path d="M6 6v11"/><path d="M10 3v18"/><path d="M14 8v7"/><path d="M18 5v13"/><path d="M22 10v3"/></svg>

Feel free to add double that amount  of voice pebbles + the pebbles should move up and down as if you were talking (use math for that, it shold actually look real, sometimes very small, then very fast very big, etc.)

On the right, we should have all the data and the history. On the left, we should have the transcript. Or in our case, the transcript and data sidebars should be a bit bigger, and the middle sidebar at the middle section should be slightly smaller, I would say one-third, one-third, one-third. But of course, you can install the resizable from shadcn, so we can resize those two. By default, they should have, should this be each one-third of the width, 

And top bar can also be removed. Or actually, we should keep it, but it's for now just an X button. In the middle, we could display who we're currently calling. I think that should be it.

Of course, we have the button for history that we can expand. On the right, next to the X button, we have a transcript button that we can click on to open the transcript sidebar and closing it again.

Also, on this transcript sidebar, I think since right now we display the outreach task and the mandatory disclosure, I would just combine those two in the system front. That should be added to the transcript at the top. We have three users: system, agent, and then the candidate we're calling. The claim should be in the history tab. You're right, it should be expandable. And then the evidence should also be inside of the expandable card.

Everything clear, or do you have some questions?

Feel free to use openMCP on this link, for example, and on the other design references linked in the design doc https://mobbin.com/screens/5de031ac-ea60-46c7-869a-32b443d38647?content_type=screens&sort=trending&filter=screenPatterns.Call

Feel free to use sub-agents

All clear before you imeplemtn? Just summarize again what i want you to build

---

Yes, the first message in the transcript is the system prompt, basically.

At the top bar:
- On the left, we have the superpower toggle.
- In the middle, we have the candidate name.
- On the right, we have both transcript toggle and close built in.

Looks good. Implement.

---

<system_instruction>
The user has attached these files. Read them before proceeding.
- .context/attachments/d8h2Od/image.png (93.5 KB)
</system_instruction>



That's good but switch transcript and history UI. In the history make it look like a linear activity tab where we have a pebble per action and then we connect those pebbles using hairlines. 

Also the transcript should look more like this: .context/attachments/d8h2Od/image.png 

System + cancidate don't have message bubble, only agent has one

Apart from that looks good.

run over [emil-design-eng](/Users/lorenzkrinner/.agents/skills/emil-design-eng/SKILL.md)  one more time in the end

---

Agent and candidate should be reversed actually, Candidate should have a bubble, and the agent shouldn't. The agent shouldn't have a profile picture either. And the candidate, you should actually use the name of the candidate.

---

Oh, yeah, and it wouldn't display the phone number in the transcript header. Instead, just remove it there.

In our history on the right side, display the timestamps. "Claim filed" - the badge should be next to the "Claim filed" title.

---

System name should have same styling, not white, no separator. Also create a new sidebar class that removes the background of the sidebar and makes the sidebar color the muted color.